{
  "name": "liquid-glass-llm-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "typecheck": "npm run --workspaces typecheck",
    "test": "npm run --workspace packages/contracts build && npm run --workspace packages/domain build && node --test --experimental-specifier-resolution=node"
  }
}
